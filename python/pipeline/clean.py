"""Clean and normalize raw game documents.

Extracts description, genres, tags, and top reviews, removes HTML and whitespace,
and prepares a single text field for embedding.
"""

def main():
    print("Clean placeholder – implement text normalization.")

if __name__ == "__main__":
    main()
