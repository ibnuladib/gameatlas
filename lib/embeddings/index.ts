export async function embedDocument(text: string): Promise<number[]> {
  // Placeholder: call Python embedding script or compute locally.
  // In production, this runs offline – not used at runtime.
  console.warn('embedDocument called at runtime – replace with offline pipeline result.');
  return [];
}
